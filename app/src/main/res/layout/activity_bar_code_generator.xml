<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    tools:context=".BarCodeGeneratorActivity">

    <EditText
        android:id="@+id/inputDataEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter data for barcode"/>

    <Button
        android:id="@+id/generateBarcodeButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Generate Barcode"
        android:layout_below="@id/inputDataEditText"
        android:layout_marginTop="16dp"/>

    <ImageView
        android:id="@+id/barcodeImageView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/generateBarcodeButton"
        android:layout_marginTop="16dp"
        android:scaleType="fitCenter"/>

</RelativeLayout>